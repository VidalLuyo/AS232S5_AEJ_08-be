# API-AI

Backend para detección de edad usando Spring Boot.

## Instalación

```bash
mvn clean install
mvn spring-boot:run
```

## Docker

```bash
docker build -t api-ai .
docker run -p 8080:8080 api-ai
```